<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="java.util.List"/>

        <variable
            name="tvShowviewmodel"
            type="com.loktra.tvshowapp.viewmodel.TVShowViewModel"/>

        <variable
            name="index"
            type="int"/>

    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/dimen_10">

        <android.support.v7.widget.CardView
            android:id="@+id/card_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center">

            <ImageView
                android:id="@+id/tvshow_poster"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/milky_way"/>

            <TextView
                android:id="@+id/tvshow_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{tvShowviewmodel.responseList.get(index)}"
                android:textColor="@android:color/white"
                android:layout_gravity="bottom"
                android:padding="@dimen/dimen_8"
                android:textSize="@dimen/cardview_title_size"/>
        </android.support.v7.widget.CardView>

        <TextView
            android:id="@+id/tvshow_rating"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableStart="@drawable/rating"
            android:drawableLeft="@drawable/rating"
            android:drawablePadding="@dimen/dimen_8"
            android:padding="@dimen/dimen_8"
            android:text="Rating"
            android:textSize="@dimen/rating_size"
            android:gravity="center_vertical"/>

        <TextView
            android:id="@+id/tvshow_genre"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/tvshow_genre"
            android:textSize="@dimen/rating_size"/>
    </LinearLayout>

</layout>